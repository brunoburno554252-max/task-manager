Kanban v9 - Cross-container + Same-column reordering
- 3 colunas: Pendente (16), Em Andamento (6), Concluída (10)
- Cards com prioridade, título, descrição, responsável, data
- Sem erros TypeScript, servidor rodando
- Backend tem campo sortOrder e rota reorder para persistir a ordem
- handleDragOver agora trata tanto cross-container quanto same-column reordering
- handleDragEnd persiste a ordem via reorderMutation
